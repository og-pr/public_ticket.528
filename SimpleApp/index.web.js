import React from 'react';
import App from './app/components/App/'; // new = part of monorepo
//import App from './app/web/'; // old = uses old folder structure where 'web' is not part of monorepo

// Render is @ App = not needed here
//ReactDOM.render(<App/>, document.getElementById('root')); // is loaded @ app/web/index.js
